import PlaceholderPage from "@/components/PlaceholderPage";
import { MessageCircle } from "lucide-react";
const Chat = () => <PlaceholderPage title="Chat" icon={MessageCircle} persona="seeker" />;
export default Chat;
