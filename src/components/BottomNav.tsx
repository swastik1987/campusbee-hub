// Bottom navigation removed — component is a no-op so existing imports continue to work.
const BottomNav = (_props: { persona?: "seeker" | "provider" | "platform_admin" }) => null;
export default BottomNav;
